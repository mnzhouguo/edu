import { CalendarDays } from 'lucide-react';import type { Student } from '../types';
export function DashboardPage({student,onNavigate}:{student:Student;onNavigate:(page:'today'|'week')=>void}){return <><div className="page-heading"><div><span className="eyebrow">数据概览</span><h1>{student.name}的学习工作台</h1><p>{student.school||'学校未填写'} · {student.grade||'年级未填写'}</p></div><div className="date-chip">{new Intl.DateTimeFormat('zh-CN',{year:'numeric',month:'long',day:'numeric',weekday:'long'}).format(new Date())}</div></div><div className="metrics"><article><span>今日任务</span><strong>—</strong><small>进入今日看板查看</small></article><article><span>本周完成率</span><strong>—</strong><small>提交与评价将在后续接入</small></article><article><span>积分余额</span><strong>0</strong><small>家长评价后正式入账</small></article></div><div className="workspace"><section className="panel profile"><div className="panel-head"><div><span className="eyebrow">Student Profile</span><h2>孩子档案</h2></div></div><dl><div><dt>姓名或昵称</dt><dd>{student.name}</dd></div><div><dt>年级</dt><dd>{student.grade||'未填写'}</dd></div><div><dt>学校</dt><dd>{student.school||'未填写'}</dd></div><div><dt>当前目标</dt><dd>{student.currentGoal||'暂未设置目标'}</dd></div></dl></section><section className="panel next"><span className="eyebrow">开始安排</span><h2>建立本周学习节奏</h2><p>先按科目创建一周任务，再回到今日看板调整孩子实际执行顺序。</p><div className="stack-actions"><button className="primary" onClick={()=>onNavigate('week')}><CalendarDays size={18}/>制定周计划</button><button className="secondary" onClick={()=>onNavigate('today')}>查看今日看板</button></div></section></div></>}
